import { Route } from '@angular/router';

import { RecipeContainerComponent } from './recipe-container/recipe-container.component';

export const recipeRoutes: Route[] = [{ path: '', component: RecipeContainerComponent }];
