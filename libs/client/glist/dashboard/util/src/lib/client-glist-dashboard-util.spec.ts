import { clientGlistDashboardUtil } from './client-glist-dashboard-util';

describe('clientGlistDashboardUtil', () => {
  it('should work', () => {
    expect(clientGlistDashboardUtil()).toEqual('client-glist-dashboard-util');
  });
});
