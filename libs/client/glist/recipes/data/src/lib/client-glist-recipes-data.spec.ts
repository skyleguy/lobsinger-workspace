import { clientGlistRecipesData } from './client-glist-recipes-data';

describe('clientGlistRecipesData', () => {
  it('should work', () => {
    expect(clientGlistRecipesData()).toEqual('client-glist-recipes-data');
  });
});
