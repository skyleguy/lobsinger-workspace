import { clientGlistRecipesUtil } from './client-glist-recipes-util';

describe('clientGlistRecipesUtil', () => {
  it('should work', () => {
    expect(clientGlistRecipesUtil()).toEqual('client-glist-recipes-util');
  });
});
