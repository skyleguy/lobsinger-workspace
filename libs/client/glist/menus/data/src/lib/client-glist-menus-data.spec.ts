import { clientGlistMenusData } from './client-glist-menus-data';

describe('clientGlistMenusData', () => {
  it('should work', () => {
    expect(clientGlistMenusData()).toEqual('client-glist-menus-data');
  });
});
