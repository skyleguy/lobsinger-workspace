import { clientGlistGlistsUtil } from './client-glist-glists-util';

describe('clientGlistGlistsUtil', () => {
  it('should work', () => {
    expect(clientGlistGlistsUtil()).toEqual('client-glist-glists-util');
  });
});
