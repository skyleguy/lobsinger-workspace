import { clientGlistGlistsData } from './client-glist-glists-data';

describe('clientGlistGlistsData', () => {
  it('should work', () => {
    expect(clientGlistGlistsData()).toEqual('client-glist-glists-data');
  });
});
